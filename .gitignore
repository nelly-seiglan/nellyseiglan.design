# OS
.DS_Store

# Local tooling (preview server config, agent settings)
.claude/

# Large local video sources (keep out of the repo — GitHub rejects >100MB)
*.mov

# Local design explorations (not deployed)
home-refined.html
home-wow.html
.vercel
